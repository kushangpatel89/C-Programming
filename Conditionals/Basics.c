#include<stdio.h>
int main(){

int x;

    printf("x:");
    scanf("%d",&x);

    //if Statement
    if(x>0) printf("positive value");
    if(x<0) printf("negetive value");
    if(x==0) printf("Number is zero"); //== use to check statement true or false, = use to give value

 return 0;
}
